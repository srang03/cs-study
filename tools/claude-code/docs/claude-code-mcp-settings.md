클로드 코드 MCP 설정 방법

Project 에서 mcp 추가해서 테스트 .mcp.json

.mcp.json에서 특정 mcp 값 복사 jp -c '.mcpServers.<NAME>'.mcp.json

User Scope에 저장 claude mcp add-json <NAME> 'json' -s user


1. .mcp.json 파일 생성
