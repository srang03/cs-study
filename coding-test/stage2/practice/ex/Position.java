package ct.stage2.practice;

// ### 문제 1: 좌표 클래스

// `Point` 클래스를 만들어라 (x, y 필드, 생성자, 두 점 사이 거리 계산 메서드).
// `main`에서 두 점을 생성하고 거리를 출력하라. (`Math.sqrt`, `Math.pow` 활용)

import java.math.*;

public class Position {
        
}
